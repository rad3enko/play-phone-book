# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /contacts                   controllers.ContactController.getById(id)
GET     /contacts/list              controllers.ContactController.listAll

+nocsrf
POST    /contacts/create            controllers.ContactController.create